import PagePlaceholder from "@/components/PagePlaceholder";

export default function WebhooksPage() {
  return (
    <PagePlaceholder
      title="Webhooks"
      description="Register URLs to receive HMAC-signed POSTs on conversation and message events."
    />
  );
}
