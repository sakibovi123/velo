import PagePlaceholder from "@/components/PagePlaceholder";

export default function AuditPage() {
  return (
    <PagePlaceholder
      title="Audit Logs"
      description="Security-relevant actions: invites, role changes, key revocations, settings updates."
    />
  );
}
