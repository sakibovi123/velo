import PagePlaceholder from "@/components/PagePlaceholder";

export default function CopilotPage() {
  return (
    <PagePlaceholder
      title="AI Copilot"
      description="Configure copilot prompts, tone, and source coverage. The in-conversation copilot lives in the inbox sidebar."
    />
  );
}
